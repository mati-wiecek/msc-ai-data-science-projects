# Outputs directory

This directory is used for generated files when the experiment runner is executed locally:

- `regression_submission.csv`
- `classification_submission.csv`
- `local_validation_metrics.csv`
- `model_diagnostics.json`

Generated outputs are ignored by Git so that public commits stay clean.
