# Data folder

Place the Sign Language MNIST CSV files here:

```text
data/raw/sign_mnist_train.csv
data/raw/sign_mnist_test.csv
```

The raw CSV files are intentionally excluded from GitHub by `.gitignore` to avoid committing large dataset files or files with uncertain redistribution rights.
